# deprecated - renamed to jaspr, so we can remove this from the gitignore and stop ignoring it after the rename is complete
/bin/git-jaspr

/bin/jaspr

# Claude Code - ignore local settings, keep shared settings
.claude/settings.local.json
